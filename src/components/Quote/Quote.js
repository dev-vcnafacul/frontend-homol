import { useState } from "react";
import { FlexWrapper, QuoteWrapper, SideWrapper } from "./styles";

const Quote = ({ quoteText, sideText }) => {
    return (
        <FlexWrapper>
            <QuoteWrapper>Abuble</QuoteWrapper>
            <SideWrapper>Abuble</SideWrapper>
        </FlexWrapper>
    );
};

export default Quote;
